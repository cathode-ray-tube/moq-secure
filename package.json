{
  "name": "moq-secure-monorepo",
  "private": true,
  "workspaces": [
    "js",
    "test-vectors"
  ],
  "scripts": {
    "build": "npm run build --workspace js",
    "build:browser": "npm run build:browser --workspace js",
    "generate-test-vectors": "tsx generate-js-test-vectors.ts --workspace test-vectors",
    "pretest": "npm run generate-test-vectors",
    "test": "npm test --workspace js",
    "test:watch": "npm run test:watch --workspace js",
    "test-rust": "npm run generate-test-vectors && cargo test"
    "typecheck": "npm run typecheck --workspace js",
    "vectors:generate": "npm run generate --workspace test-vectors"
  }
}
