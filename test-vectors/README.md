# Test vectors

Test vectors can be generated by running **generate-js-test-vectors.ts**:

```bash
npm run vectors:generate
```

This will populate the frames.json file in the test-vectors directory.
